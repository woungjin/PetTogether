<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="com.pet.freeboard.mapper.FreeBoardMapper">
  	
  	<insert id="myGoodInsert">
  		insert into mygood values(#{bno} , #{user_id}) 
  	</insert>
  	
  	<delete id="myGoodDelete">
  		delete from mygood where bno = #{bno} and user_id = #{user_id}
  		
  	</delete>
  
  	<insert id="fileInsert">
		insert into reviewboard(review_bno , writer , content , uploadPath, fileloca , filename , filerealname)
		values( 1, #{writer} , #{content} , #{uploadPath} , #{fileLoca} , #{fileName} ,#{fileRealName} )  		
  	</insert>
 
  
  
  </mapper>