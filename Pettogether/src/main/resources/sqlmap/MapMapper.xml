<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.pet.map.mapper.MapMapper">
  	
  	<select id="categoryCheck" resultType="StarBoardVO">
  		select * from starboard where category = #{category} order by review_avg desc
  	</select>
 
  
  
  </mapper>